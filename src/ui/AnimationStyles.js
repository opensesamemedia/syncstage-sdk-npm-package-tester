const mountedStyle = {
  animation: "inAnimation 700ms ease-in",
};
const unmountedStyle = {
  animation: "outAnimation 700ms ease-out",
  animationFillMode: "forwards",
};

export {mountedStyle, unmountedStyle};